<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:name=".APKToolApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.APKToolCompanion"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">
        
        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="apktool" />
            </intent-filter>
        </activity>

        <!-- APK Upload Activity -->
        <activity
            android:name=".ui.UploadActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion" />

        <!-- Analysis Results Activity -->
        <activity
            android:name=".ui.AnalysisResultsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion" />

        <!-- Device Management Activity -->
        <activity
            android:name=".ui.DeviceManagementActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion" />

        <!-- Authentication Activity -->
        <activity
            android:name=".ui.AuthenticationActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.APKToolCompanion" />

        <!-- Foreground Service for background analysis -->
        <service
            android:name=".services.AnalysisService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Network Receiver -->
        <receiver
            android:name=".receivers.NetworkReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>